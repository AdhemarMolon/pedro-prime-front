# ========================================
# 🗄️ NEON DATABASE (PostgreSQL)
# ========================================

# ⚠️ IMPORTANTE: Configure estas variáveis no Vercel Dashboard
# Vercel > Seu Projeto > Settings > Environment Variables

# Connection String Direta (APENAS para migrations locais)
DATABASE_URL="postgresql://user:password@host.neon.tech/database?sslmode=require"

# Connection String Pooled (PRODUÇÃO - Configure no Vercel!)
# Esta é a que o Vercel deve usar em runtime
DATABASE_URL_POOLED="postgresql://user:password@host-pooler.neon.tech/database?sslmode=require&pgbouncer=true&connect_timeout=15"

# ========================================
# 🔐 SEGURANÇA
# ========================================

# JWT Secret (para autenticação admin)
# ⚠️ OBRIGATÓRIO: Gere um aleatório em: https://generate-secret.vercel.app/32
JWT_SECRET="gere-um-secret-aleatorio-seguro"

# ========================================
# 🌐 API E FRONTEND
# ========================================

# URL do seu site (Vercel preenche automaticamente)
VITE_SITE_URL="https://seu-dominio.vercel.app"

# URL Base da API
# Produção Vercel: deixe VAZIO "" (usa mesmo domínio)
# Desenvolvimento: "http://localhost:3001"
VITE_API_URL=""

# ========================================
# 🔧 DESENVOLVIMENTO
# ========================================

NODE_ENV="development"

# ========================================
# 📝 COMO USAR
# ========================================
#
# 1. Copie este arquivo para .env
#    cp .env.example .env
#
# 2. Preencha com suas credenciais do Neon
#    - Acesse: https://neon.tech
#    - Copie a connection string
#
# 3. Gere um JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 4. Execute as migrations:
#    npx prisma generate
#    npx prisma migrate dev --name init
